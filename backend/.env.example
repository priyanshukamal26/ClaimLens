# ClaimLens Nexus — Environment Variables
# Copy to .env and fill in actual values

# LLM Configuration (ADR-003: 4-stage fallback chain)
# Per AI_ML.md: NEVER hardcode model IDs — confirm in-console each session
GROQ_API_KEY=
GROQ_MODEL_ID=
GEMINI_API_KEY=
GEMINI_MODEL_ID=

# Application
DEBUG=true
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
